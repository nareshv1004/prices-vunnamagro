[build]
  command   = "npm run build"
  publish   = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[functions."price-data"]
  timeout = 30

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
